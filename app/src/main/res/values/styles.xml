<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="textHeading" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/colorBlackTitle</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@font/poppins_semibold</item>
    </style>

    <style name="textSubDescription" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/colorBlackTitle</item>
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@font/poppins_regular</item>
    </style>
</resources>
